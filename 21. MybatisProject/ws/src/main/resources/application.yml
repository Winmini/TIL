spring:
  messages:
    basename: errors

  datasource:
    url: jdbc:mysql://127.0.0.1:3306/ssafyweb?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    username: ssafy
    password: ssafy
    driver-class-name: com.mysql.cj.jdbc.Driver

  mybatis:
    type-aliases-package: com.example.myproject.mapper
    mapper-locations: mybatis/mapper/**/*.xml

  data:
    web:
      pageable:
        default-page-size: 10
        max-page-size: 50

logging.level:
  org.hibernate.SQL: debug

server.servlet.session:
  tracking-modes: cookie
  timeout: 1800